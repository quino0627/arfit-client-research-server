# Migration `20200423150639-init`

This migration has been generated by quino0627 at 4/23/2020, 3:06:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `arfit-research`.`User` (
    `createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    `id` int NOT NULL  AUTO_INCREMENT,
    `lastLoginAt` datetime(3)   ,
    `loginWhere` varchar(191)   ,
    `name` varchar(191) NOT NULL  ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `arfit-research`.`Quest` (
    `id` int NOT NULL  AUTO_INCREMENT,
    `userId` int  ,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `arfit-research`.`Exercise` (
    `id` int NOT NULL  AUTO_INCREMENT,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `arfit-research`.`Machine` (
    `id` int NOT NULL  AUTO_INCREMENT,
    PRIMARY KEY (`id`)
) 
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `arfit-research`.`Quest` ADD FOREIGN KEY (`userId`) REFERENCES `arfit-research`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200423150639-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id          Int       @default(autoincrement()) @id
+  name        String
+  createdAt   DateTime  @default(now())
+  lastLoginAt DateTime?
+  loginWhere  String?
+  QuestList   Quest[]
+}
+
+model Quest {
+  id     Int  @default(autoincrement()) @id
+  userId Int?
+}
+
+model Exercise {
+  id Int @default(autoincrement()) @id
+}
+
+model Machine {
+  id Int @default(autoincrement()) @id
+}
```


